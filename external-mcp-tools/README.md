## External MCP tools

This folder contains third-party MCP servers and deployment assets that can be enabled per workspace via **IntegrationBits**.

### Structure

- `excalidraw-mcp/`
  - `upstream/`: cloned upstream MCP server source
  - `deploy/`: Workbit-owned Docker/EC2 deployment assets

