@thedatablitz:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${WORK_BIT_GH_TOKEN}
# @thedatablitz/comment peers @lexical/code@^0.35; app uses Lexical 0.42 — install until packages align.
legacy-peer-deps=true
